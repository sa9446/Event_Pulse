# Robolectric defaults to NATIVE SQLite on modern SDKs, which fails on Windows
# with SQLiteCantOpenDatabaseException (code 14 SQLITE_CANTOPEN). The LEGACY
# implementation uses the in-process Java SQLite and works everywhere.
sqliteMode=LEGACY
